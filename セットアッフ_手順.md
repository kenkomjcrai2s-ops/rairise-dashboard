# らいらいず 個人成績ダッシュボード（Firebase連携版）セットアップ手順

このフォルダには以下が入っています。

- `index.html` … 閲覧画面＋管理者画面が一体になったアプリ本体
- `seed_data.json` … 直近アップロードいただいたxlsxから変換した初期データ（51名分）

デザイン・計算ロジックはこれまでのHTML版と同じです。違いは「データの保存場所が
埋め込みJSONではなく、Firestore（クラウド上のデータベース）になった」ことだけです。
これにより、管理者が半荘結果を入力すると、全員の画面にリアルタイムで反映されます。

---

## 1. Firebaseプロジェクトを作る（無料）

1. https://console.firebase.google.com/ にアクセスし、Googleアカウントでログイン
2. 「プロジェクトを追加」→ 適当な名前（例: `raiseraise-mahjong`）を入力 → 作成
   （Googleアナリティクスは「有効にしない」でOK）
3. 左メニューの「構築」→「Firestore Database」→「データベースの作成」
   - ロケーションは `asia-northeast1`（東京）などお好みで
   - セキュリティルールは最初は「テストモード」で作成でOK（後述の手順4で調整します）
4. 左メニューの「プロジェクトの概要」の歯車アイコン →「プロジェクトの設定」
5. 下にスクロールし「マイアプリ」→ `</>`（ウェブ）アイコンをクリックしてアプリを登録
   （名前は何でもOK。Firebase Hostingは使わないので「追加」だけでよい）
6. 表示された `firebaseConfig = { apiKey: "...", ... }` の中身を丸ごとコピー

## 2. index.html にFirebaseの設定を貼り付ける

`index.html` を開き、以下の部分を探して、手順1-6でコピーした値に置き換えてください。

```js
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

同じ場所のすぐ下にある合言葉も、運用開始前に必ず変更してください。

```js
const ADMIN_PASSWORD = "CHANGE_ME_BEFORE_DEPLOY";  // ← 好きな合言葉に変更
```

## 3. Firestoreのセキュリティルールを設定する

Firebaseコンソール →「Firestore Database」→「ルール」タブで、以下に置き換えて「公開」してください。

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

※ このルールは「誰でも読み書きできる」設定です。管理者ページの合言葉はあくまで
「ボタンを間違って押さない」ための簡易的なものであり、URLとFirebaseの設定値さえ分かれば
理論上は誰でもデータを書き換えられます。身内サークルの成績表という前提で許容いただく想定です。
もし将来的にちゃんとしたログイン制御をしたくなったら、Firebase Authenticationの導入をご提案できます。

## 4. GitHub Pages（またはNetlify）に公開する

### GitHub Pagesの場合
1. GitHubで新しいリポジトリを作成
2. `index.html` と `seed_data.json` をそのリポジトリにアップロード
3. リポジトリの「Settings」→「Pages」→ Sourceを `main` ブランチ・`/ (root)` に設定して保存
4. 数分後、`https://ユーザー名.github.io/リポジトリ名/` でアクセス可能に

### Netlifyの場合
1. https://app.netlify.com/ で「Add new site」→「Deploy manually」
2. `index.html` と `seed_data.json` が入ったフォルダをドラッグ&ドロップ
3. 発行されたURLでアクセス可能に

## 5. 初期データを取り込む（最初の1回だけ）

1. 公開されたページを開き、「管理者」タブへ
2. 合言葉でログイン
3. 一番下の「初回データ取込」→「seed_data.json を取り込む」ボタンを押す
4. 「51名分のデータを取り込みました」と表示されればOK

これで全員が最新のxlsxから移行した状態でスタートできます。
**この取込は最初の1回だけ実行してください**（2回目以降は「既にメンバーが登録されているため中止」となり、二重登録は防止されます）。

## 6. 今後の運用

- 対局後、管理者が「管理者」タブ→「半荘結果を入力」で4人分の着順・得点を入力して登録
- 登録すると全員の画面（閲覧タブ）に即座に反映される
- 新しいメンバーが増えたら「メンバーを追加」で登録（初期段位は五級がデフォルト、変更可）
- 入力ミスをした場合は「直近の入力履歴」から該当の半荘を「取り消す」ことで、その半荘登録前の状態にきれいに戻せます（直近5件まで表示。古い履歴は一覧に出ませんが、Firestore上には残っています）
- **xlsxでの管理は不要になります。** 今後はこの画面だけで完結します。

## 既知の制約・今後の相談ポイント

- 合言葉ログインは「簡易的な入店チェック」であり、本格的なアクセス制御ではありません。
- 半荘結果の入力は必ず「4人1組」で登録する前提です（通常の東南戦・半荘想定）。
- 「取り消し」は直近の入力を1件ずつ、正しい手順で取り消した場合のみ想定通り動作します
  （複数人が同時に入力するなど特殊なタイミングでは、想定外の挙動になる可能性があります）。
- ポイント（収支）の単位は、これまでの個人タブに入っていた「実績」列と同じ単位（生スコア差分）に
  統一しました。xlsx時代の「一覧」シートのポイント表記（例: +59.6）とはスケールが異なりますので、
  今後入力する得点は「実績列と同じ感覚の数値」で入力してください。
